# Lesson 1 Exercise 5: Mixed Topology

name: mixed-topology

topology:
  nodes:
    router:
      kind: srl
      image: ghcr.io/nokia/srlinux:24.10.1

    host1:
      kind: linux
      image: alpine:3.20
  
  links:
    - endpoints: ["router:e1-1", "host:eth0"]
