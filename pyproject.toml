[project]
name = "kubecraft"
version = "0.1.0"
description = "Network fundamentals training course using containerlab"
requires-python = ">=3.10"

[dependency-groups]
test = ["pytest>=8.0", "pyyaml>=6.0"]
